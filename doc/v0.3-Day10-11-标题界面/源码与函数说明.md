# v0.3 Day10–11 源码与函数说明

## 1. 流程

```
Main (GDScript _ready)
  → title_screen.tscn
       [开始旅程] → birthday_gate.tscn
                      [进入森林] → Main.begin_run() → load_room(0)
       [退出] → get_tree().quit()
```

## 2. 关键文件

| 路径 | 说明 |
|------|------|
| `project/scripts/title_screen.gd` | 标题 UI + 动效 |
| `project/scenes/ui/title_screen.tscn` | CanvasLayer |
| `project/scripts/main.gd` | 启动挂标题（不再直接挂生日门） |
| `project/assets/art/title/title_bg.png` | 可选背景 |

## 3. 改名

`title_screen.gd`：

```gdscript
const GAME_TITLE := "林间回响"
const TAGLINE := "..."
```

## 4. 背景加载

`ResourceLoader.exists(TITLE_BG_PATH)` 为真时用 `TextureRect` `STRETCH_KEEP_ASPECT_COVERED` 全出血 + 半透明绿纱；否则纯色占位。
